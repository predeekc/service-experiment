web:
  build: ./web
  links:
    - "auth:auth.sample.local"
auth:
  build: ./auth-server
router:
  build: ./router
  ports:
    - "80:80"
    - "443:443"
  links:
    - "web:servers_web_01"
    - "auth:servers_auth_01"
  environment:
    - SERVERS_WEBPORT=80
    - SERVERS_AUTHPORT=80
